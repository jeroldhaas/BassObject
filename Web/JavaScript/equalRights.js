// JavaScript source code
var equalRights = function () {
    var reject = ["Racism", "Sexism", "Ageism", "Homophobia"];

    for (i = 0; i < reject.length; i++) {
        alert("I reject " + reject[i] + "!");
    }
};